基于用户的协作型过滤
基于物品的协作型过滤(物品间的比较不会像用户那样频繁)


索引的数据结构
	list<byte>来保存

	函数merge，多个lis，进行merge
	建立map来保存结果
	
获取size
nextIndex = 3
	nextIndex = (nextIndex + 1) % size;
	
updateLogBean
	softid
	name
	date
	function
	 