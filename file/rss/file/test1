多版本并发控制：
	MVCC，行加锁的变形，避免了很多情况下的加锁操作，降低了系统的开销
	InnoDB为每个数据行添加两个隐含值的方式来显示mvcc，记录了行的创建时间、以及他们的过期时间
myisam表可以包含动态行和静态行，mysql会根据表定义选择合适的类型
 	check table ,repair table
 	压缩myisam表
 		对于数据永不改变的，可以将表进行压缩，从而可以减少磁盘的空间
 	延迟更新索引 delay_key_write,不会将索引改变数据写入磁盘，对于数据经常
 		改动且使用频繁的表，可以提高速度